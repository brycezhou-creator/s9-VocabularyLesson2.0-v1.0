# Phase 4: 智能复习与容错 - 功能说明

## 概述

Phase 4 是智能复习与容错系统，基于艾宾浩斯遗忘曲线自动推送复习任务，并通过三级容错机制保证学习效果。

## 核心特性

### 1. 艾宾浩斯曲线复习推送

- **自动调度**：系统根据学生的学习状态，自动计算下次复习时间
- **复习周期**：
  - 第1次复习：3天后
  - 第2次复习：7天后
  - 第3次及以后：30天后
- **出库条件**：复习5次 + 持续30天 → 转为绿灯（Green）

### 2. 三级容错机制

Phase 4 采用渐进式容错策略，避免学生因一次小错就被标记为红灯：

#### Level 1: 手滑提示 🟡
**触发条件**：第一次拼写错误，且相似度 ≥ 80%

**系统反馈**：
- 震动反馈 + 气泡提示："拼写有一点小误差，再检查一下～"
- 不判错，不降级
- 给予第二次机会

**学生操作**：修正后重新提交

**结果**：
- 修正成功 → 完美保黄，正常进度
- 修正失败 → 进入 Level 2

---

#### Level 2: 降级助推 🟠
**触发条件**：
- Level 1 修正后仍然错误
- 第一次拼写错误，相似度 < 80%

**系统反馈**：
- 题目降级为 L3（骨架提示）
- 显示单词骨架：例如 `a d a p t` 或 `c h a l _ e n g e`
- 提示："给你一点线索，能想起来吗？"

**学生操作**：根据骨架填写完整单词

**结果**：
- 填对 → 勉强保黄，但复习周期重置为 Day 1
- 填错 → 进入 Level 3 熔断

---

#### Level 3: 熔断锁定 🔴
**触发条件**：Level 2 骨架提示后仍然填错

**系统反馈**：
- 显示正确答案
- 标记为红灯（Red）
- 提示："没关系，这个词我们下节课找老师修。"

**学生操作**：无需操作，自动进入下一个单词

**结果**：
- 单词进入 Red Box，等待老师修复

---

## 使用流程

### 1. 进入复习

访问 Demo 页面，点击"智能复习与容错"卡片，进入 Phase 4。

### 2. 复习任务

- 系统显示需要复习的单词（黄词列表）
- 顶部显示进度条：当前进度 / 总任务数
- 每个单词显示：
  - 语境例句
  - 中文释义
  - 输入框

### 3. 拼写提交

1. 在输入框中输入单词拼写
2. 点击"提交"按钮或按 Enter 键
3. 系统判断拼写正确性，给予对应反馈

### 4. 容错处理

根据三级容错机制，系统会给予不同程度的提示：

- **完全正确**：显示"✓ 正确，完美保黄"，2秒后自动进入下一个单词
- **Level 1 错误**：震动提示，可再次尝试
- **Level 2 错误**：显示骨架提示
- **Level 3 熔断**：显示正确答案，标记红灯，3秒后进入下一个单词

### 5. 完成复习

- 所有单词复习完成后，显示完成界面
- 统计本次复习的单词数量
- 提示下次复习时间

---

## 技术实现

### 数据结构

#### 单词状态（Word State）
```javascript
{
  wordId: 1,
  status: 'yellow',           // 'red' | 'yellow' | 'green'
  reviewCount: 2,             // 复习次数
  lastReviewTime: '2026-01-22T10:00:00Z',
  nextReviewTime: '2026-01-25T10:00:00Z',
  errorCount: 0,              // 错误次数
  errorPatterns: [],          // 错误拼写记录
  phase: 4
}
```

### 相似度算法

使用 **Levenshtein 距离算法**计算拼写相似度：

```javascript
similarity = 1 - (distance / maxLength)
```

- `similarity >= 0.8`：判定为 Level 1 手滑
- `similarity < 0.8`：判定为 Level 2 降级

### 骨架生成算法

```javascript
generateSkeleton(word) {
  // 显示首字母、尾字母和部分中间字母
  // 每隔一个字母显示，其他用下划线替代
  // 例如：adapt → a d a p t
  // 例如：challenge → c h a l _ e n g e
}
```

---

## Store 状态管理

### useWordStore

位置：`src/shared/store/useWordStore.js`

**核心方法**：

- `reviewSuccess(wordId)` - 复习成功，正常推进复习周期
- `reviewSuccessWithReset(wordId)` - 勉强保黄，复习周期重置为 Day 1
- `reviewFailToRed(wordId, errorPattern)` - 熔断锁定，转为红灯

**状态转换**：

```
Yellow + 复习成功 → Yellow（复习次数+1）
Yellow + 勉强保黄 → Yellow（复习次数重置为0）
Yellow + 熔断失败 → Red（等待老师修复）
Yellow + 5次复习 + 30天 → Green（永久出库）
```

---

## UI 组件

### 主要组件

- `SmartReview.jsx` - 主组件
- `SmartReview.css` - 样式文件

### 关键样式

- `.review-feedback--level1` - Level 1 黄色提示
- `.review-feedback--level2` - Level 2 橙色提示
- `.review-feedback--level3` - Level 3 红色提示
- `.shake` - 震动动画

---

## 测试场景

### 场景 1：完美保黄

1. 输入正确拼写：`adapt`
2. 点击提交
3. 显示"✓ 正确，完美保黄"
4. 2秒后进入下一个单词

### 场景 2：手滑提示（Level 1）

1. 输入轻微错误：`adpt`（少了一个a，相似度80%+）
2. 点击提交
3. 震动 + 提示："拼写有一点小误差，再检查一下～"
4. 修正为：`adapt`
5. 点击提交
6. 显示"✓ 正确，完美保黄"

### 场景 3：降级助推（Level 2）

1. 输入错误拼写：`adatp`（相似度<80%）或 Level 1 修正后仍错
2. 点击提交
3. 显示骨架提示："a d a p t"
4. 根据骨架填写：`adapt`
5. 点击提交
6. 显示"✓ 正确，勉强保黄"（复习周期重置为 Day 1）

### 场景 4：熔断锁定（Level 3）

1. Level 2 骨架提示后仍然填错
2. 点击提交
3. 显示"✗ 熔断锁定 🔴"
4. 显示正确答案：`adapt`
5. 提示："没关系，这个词我们下节课找老师修。"
6. 3秒后自动进入下一个单词

---

## 访问方式

1. 启动开发服务器：
```bash
npm run dev
```

2. 打开浏览器访问：`http://localhost:5173`

3. 在首页点击"智能复习与容错"卡片

---

## 依赖模块

- `zustand` - 状态管理
- `lucide-react` - 图标库
- `react-router-dom` - 路由

---

## 后续优化方向

1. **音频反馈**：添加正确/错误的音效
2. **统计报告**：显示复习准确率、平均用时等
3. **自定义复习**：允许学生手动选择要复习的单词
4. **AI 辅助**：集成 AI 语音识别，支持口语拼写
5. **艾宾浩斯曲线可视化**：图表展示复习进度和遗忘曲线

---

## 联系与反馈

如有问题或建议，请联系开发团队。

**文档更新时间**：2026-01-26
